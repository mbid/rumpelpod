Add automatic SSH agent configuration

Allow pods to use configured key files or the caller's ambient SSH agent
while keeping ambient selection resilient across overlapping forwarded
SSH sessions. Tie managed agent lifetime to PodConnection so stopped,
reconnected, and recreated pods reuse it, while daemon restart restores
configured agents. Fail lifecycle operations before pod creation when
agent startup or key loading fails.
