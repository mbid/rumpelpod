Pin prepared Grok to host CLI version

Install the host Grok CLI version in prepared pods
instead of a rumpelpod-pinned release. Add the
version to image identity so rebuilding a pod
cannot silently reuse a different Grok binary.
