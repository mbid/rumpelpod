Fix Grok smoke cache drift

Normalize Grok's user-info date line when computing LLM cache keys so
the smoke fixture remains valid across days. Regenerate the Grok smoke
cache with a live response sequence and document that cache responses
should be refreshed through the proxy instead of hand-edited.
