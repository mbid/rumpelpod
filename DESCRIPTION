Add automatic SSH agent configuration

Allow pods to use configured key files or the caller's ambient SSH agent
without adding daemon round trips. Keep ambient agent selection
resilient across overlapping forwarded SSH sessions.
