Restore Codex 0.147 code mode support

Keep the pinned test release separate from the developer's Codex, and
install the CLI plus its required code-mode host directly from GitHub
release archives without adding download tools to prepared images.
Normalize generated Codex message IDs so the updated smoke fixtures
continue to replay offline, and exercise a real shell tool call so a
missing code-mode host fails behaviorally.
