Resume Codex threads when relaunching a frontend

Launch Codex frontends with `resume --last` against the pod's
app-server.
Codex resumes an available thread and starts a new one when none exists.
Preserve the existing fast path for attaching to a live frontend.

Cover recovery with an integration test that exits the first TUI and
verifies the relaunched frontend restores its remote conversation.
